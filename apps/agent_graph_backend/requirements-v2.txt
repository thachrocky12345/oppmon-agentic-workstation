# Minimal requirements for the v2-only server (mindsearch.v2_server).
# Replaces the bloated requirement.locked.txt that pinned legacy deps
# (torch, jupyter*, streamlit, gradio, transformers, lmdeploy, ...).
#
# Bump versions deliberately — every change should be tested against
# the /solve_v2 contract documented in docs/solve-v2.md.

# --- HTTP server ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
sse-starlette==2.1.3

# --- Validation + settings ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- LLM SDKs (lazy-imported; pick what your LLM_PROVIDER points at) ---
anthropic==0.42.0
openai==1.59.6

# --- HTTP client for Google Custom Search ---
httpx==0.28.1

# --- DuckDuckGo web search (renamed from duckduckgo-search) ---
ddgs==9.0.0

# --- .env loading ---
python-dotenv==1.0.1

# --- Postgres (TAG-51) — async driver for JWT verify, model registry, corpus search ---
asyncpg==0.30.0

# --- JWT (TAG-52) — HS256 token verification matching apps/api (jsonwebtoken) ---
PyJWT==2.10.1

# --- Secret vault (TAG-54) — XSalsa20-Poly1305 matching apps/api (tweetnacl) ---
PyNaCl==1.5.0
