# Language tactics

- Use language tooling as the source of truth (linters, formatters, type checks).
- Locate framework-specific boot files and routing configuration.
- Prefer idiomatic patterns (DI, middleware, ORM) before exploring custom wiring.
- Watch for generated files; treat them as reference, not documentation.
- Cross-check conventions across multiple modules to avoid local exceptions.

Hints / next steps: If unsure, search the codebase for common framework keywords and confirm with config files.
