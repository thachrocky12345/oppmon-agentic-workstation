# Technical: Location-based navigation content

## Screens / routes
- Not found in repo. Search evidence: `rg -n "location-based" RG-Frontend/src` (0 matches)

## Frontend components/modules
- Not found in repo. Search evidence: `rg -n "location-based" RG-Frontend/src` (0 matches)

## Backend apps/modules
- Not found in repo. Search evidence: `rg -n "location-based" Lumy-Backend/apps` (0 matches)

## APIs / GraphQL operations
- Not found in repo. Search evidence: `rg -n "location-based" Lumy-Backend/apps` (0 matches)

## Key files and directories
- Not found in repo. Search evidence: `rg -n "location-based" RG-Frontend/src` (0 matches)
- Not found in repo. Search evidence: `rg -n "location-based" Lumy-Backend/apps` (0 matches)

## Tests
- Not found in repo. Search evidence: `rg -n "location-based" RG-Frontend/src` (0 matches)

## Config / env
- Lumy-Backend/lumy_global/settings.py
- RG-Frontend/src/store/axiosInstance.ts

## Known risks / open questions
- Not found in repo. Search evidence: `rg -n "location-based" RG-Frontend/src` (0 matches)

## Source docs
- ContextFiles/HumanDocuments/Features/_extracted/List of Features.txt
