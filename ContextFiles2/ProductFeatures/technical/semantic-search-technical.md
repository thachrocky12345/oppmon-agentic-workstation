# Technical: Semantic Search

## Screens / routes
- Not found in repo. Search evidence: `rg -n "semantic" RG-Frontend/src` (0 matches)

## Frontend components/modules
- Not found in repo. Search evidence: `rg -n "semantic" RG-Frontend/src` (0 matches)

## Backend apps/modules
- Not found in repo. Search evidence: `rg -n "semantic" Lumy-Backend/apps` (0 matches)

## APIs / GraphQL operations
- Not found in repo. Search evidence: `rg -n "semantic" RG-Frontend/src/graphql Lumy-Backend/apps` (0 matches)

## Key files and directories
- Not found in repo. Search evidence: `rg -n "semantic" RG-Frontend/src Lumy-Backend/apps` (0 matches)

## Tests
- Not found in repo. Search evidence: `rg -n "semantic" Lumy-Backend RG-Frontend` (0 matches)

## Config / env
- Not found in repo. Search evidence: `rg -n "ENV|env|process.env" RG-Frontend Lumy-Backend/lumy_global` (0 matches)

## Known risks / open questions
- Not found in repo. Search evidence: `rg -n "Open Questions|Risks" ContextFiles/HumanDocuments/Features/_extracted/BRD - Semantic Search.txt` (0 matches)

## Source docs
- ContextFiles/HumanDocuments/Features/_extracted/BRD - Semantic Search.txt
