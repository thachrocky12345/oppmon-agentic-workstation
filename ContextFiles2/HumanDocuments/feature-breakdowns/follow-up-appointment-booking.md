# Feature: Easier Follow-up Appointment Booking

## Overview
Listed without a BRD. Likely a workflow for providers and clients to schedule follow-up sessions during or after a call.

## Current state (repo)
- Booking flows exist, but no direct "follow-up" scheduling shortcut is visible.

## Missing pieces
- UX for in-session or post-session booking.
- Backend logic to pre-fill provider/client context.

## Next steps
1. Define desired user flow (during session vs after session).
2. Implement shortcut UI in session or messaging interface.
3. Ensure availability and payment rules are respected.
